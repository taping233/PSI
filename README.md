# PSI
## direct_fft_no_bucket_plain32

The `direct_fft_no_bucket_plain32/` directory contains the paper-aligned Protocol 3 / Method 2 PSI benchmark, the deployable multi-process Method 2 state tool, and the C++/NTL/FLINT polynomial backend for dynamic updates.
